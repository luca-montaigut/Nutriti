<%= form_with(model: users_day, local: true) do |form| %>
  <% if users_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_day.errors.count, "error") %> prohibited this users_day from being saved:</h2>

      <ul>
      <% users_day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
